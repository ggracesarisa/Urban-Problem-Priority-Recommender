# --- 1. DATA FILES (CRITICAL) ---
# Ignore large raw data and generated files
data/*.csv
data/*.parquet
data/*.json
data/*.geojson
data/*.zip
data/*.gz

# Exception: Keep the 'data' folder structure itself
# (You need to create an empty file named .gitkeep in data/ for this to work)
!data/.gitkeep

# --- 2. ENVIRONMENT & SECRETS ---
# Never commit API keys or passwords
.env
.env.local

# --- 3. PYTHON & JUPYTER ---
__pycache__/
*.py[cod]
.ipynb_checkpoints/
*.ipynb  # Optional: Uncomment if you don't want to commit notebooks

# --- 4. AIRFLOW & DOCKER ---
logs/
plugins/
airflow-worker.pid
airflow-webserver.pid

# --- 5. IDE SETTINGS ---
.vscode/
.idea/
.DS_Store